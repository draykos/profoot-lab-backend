HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Set to true when running behind a reverse proxy that terminates TLS (e.g. Render).
IS_PROXIED=false

# Bunny.net storage zone used as the media library upload provider (shared by
# localhost and production so uploaded files stay in sync between environments).
# Values come from the Bunny.net dashboard > Storage > <zone> > FTP & API Access.
BUNNY_API_KEY=tobemodified
BUNNY_STORAGE_ZONE=tobemodified
BUNNY_HOSTNAME=tobemodified
BUNNY_PULL_ZONE=tobemodified
# Optional subfolder inside the storage zone, no leading/trailing slash.
BUNNY_UPLOAD_PATH=
